/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,

  // You can add more options here later, for example:
  // images: {
  //   remotePatterns: [
  //     { protocol: "https", hostname: "images.example.com" },
  //   ],
  // },

  // No experimental.appDir – the /app router is stable and enabled by default in Next 15.
};

export default nextConfig;
